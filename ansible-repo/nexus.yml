---

# Installation and configuration of nexus server

- hosts: nexus
  user: root
  become: yes
  become_method: sudo
  connection: ssh
  roles:
     - epel-repo
     - iptables
     - java
     - nexus
     - selinux
